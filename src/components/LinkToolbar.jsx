import React from 'react';

const LinkToolbar = () => {
    // Placeholder component for link management functionality
    // This would be implemented with selection detection and link creation/editing
    return null;
};

export default React.memo(LinkToolbar); 